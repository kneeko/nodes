Collidable = class{
	init = function(self)

		-- fallback types
		self.layers = {}

		-- bound
		-- polygon
		-- circle

		-- add to collision manager

		-- how do we get 

		-- grid?
	end,

	resolve = function(self, collision)
		-- call any methods this object may have to resolve the collision
	end,
}